[
  {
    "key": "welcome",
    "slug": "",
    "group": "get-started",
    "label": "Welcome",
    "title": "Build widgets for your game.",
    "description": "Create a widget with HTML, CSS and JavaScript, then display it in OverCrow.",
    "headings": [
      {
        "id": "un-point-de-depart",
        "title": "Start here"
      },
      {
        "id": "le-sdk-en-pratique",
        "title": "Use the SDK"
      },
      {
        "id": "plateformes",
        "title": "Windows and Linux"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<div class=\"intro-label\">CREATOR DOCUMENTATION</div>\n<p class=\"lead\">Start with a counter. Change its look, try it in your browser and import it into OverCrow.</p>\n<div class=\"hero-actions\"><a class=\"button-primary\" href=\"/docs/en/premier-widget/\">Build your first widget</a><a class=\"button-secondary\" href=\"/studio/en/\">Try the online Studio ↗</a></div>\n<div class=\"architecture-strip\"><span>HTML + CSS + JS</span><span aria-hidden=\"true\">→</span><span>OverCrow SDK</span><span aria-hidden=\"true\">→</span><span>Your in-game widget</span></div>\n<h2 id=\"un-point-de-depart\">Start here</h2>\n<div class=\"entry-grid\"><a class=\"entry-card\" href=\"/docs/en/premier-widget/\"><span class=\"card-number\">01 / START</span><h3>Make a counter</h3><p>Create a project, change it and import your first package.</p><span class=\"card-link\">Follow the guide →</span></a><a class=\"entry-card\" href=\"/docs/en/exemples/\"><span class=\"card-number\">02 / EXPLORE</span><h3>Start from an example</h3><p>Try a checklist, a market search or a performance display.</p><span class=\"card-link\">Browse examples →</span></a><a class=\"entry-card\" href=\"/docs/en/api-reseau/\"><span class=\"card-number\">03 / CONNECT</span><h3>Call an API</h3><p>Declare a permission and display a JSON response.</p><span class=\"card-link\">Read the guide →</span></a><a class=\"entry-card\" href=\"/docs/en/depannage/\"><span class=\"card-number\">04 / DEBUG</span><h3>Fix an error</h3><p>Find the cause and the next step to try.</p><span class=\"card-link\">Open troubleshooting →</span></a></div>\n<h2 id=\"le-sdk-en-pratique\">Use the SDK</h2>\n<p>The SDK connects your widget to OverCrow. For example, this call reads an online catalog:</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Read a catalog",
        "source": "import overcrow from './overcrow.js';\nconst response = await overcrow.fetch('https://api.warframe.market/v2/items');\nif (!response.ok) throw new Error('Catalog unavailable');\nconst catalog = await response.json();"
      },
      {
        "kind": "html",
        "html": "<p>Add the corresponding network permission in your manifest. <a href=\"/docs/en/api-reseau/\">See the complete example</a>.</p>\n<h2 id=\"plateformes\">Windows and Linux</h2>\n<p>The creator kit uses the same commands on Windows and Linux. See <a href=\"/docs/en/installation/\">installation and system requirements</a>.</p>\n<aside class=\"note note-info\"><strong>SDK 1.3.0</strong><p>These guides cover Web API v1 and v2. <a href=\"/docs/en/api-services/\">Explore host services</a>.</p></aside>"
      }
    ]
  },
  {
    "key": "first-widget",
    "slug": "premier-widget",
    "group": "get-started",
    "label": "First widget",
    "title": "Your first widget",
    "description": "Create a counter, preview your changes and import it into OverCrow.",
    "headings": [
      {
        "id": "prerequis",
        "title": "1. Download the kit"
      },
      {
        "id": "creer",
        "title": "2. Create a counter"
      },
      {
        "id": "personnaliser",
        "title": "3. Make a change"
      },
      {
        "id": "exporter",
        "title": "4. Build a package"
      },
      {
        "id": "tester",
        "title": "5. Test in OverCrow"
      },
      {
        "id": "ensuite",
        "title": "Next steps"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"prerequis\">1. Download the kit</h2>\n<p>Install <a href=\"https://nodejs.org/en/download\" rel=\"noreferrer\">Node.js 22 or later</a>, then download and extract the kit. Open a terminal in the extracted <code>overcrow-creator-kit</code>.</p>\n<p><a class=\"button-primary\" href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/creator-kit.zip\" download>Download the creator kit ↓</a></p>\n<p>Prefer to edit online? <a href=\"/studio/en/\">Use the Studio</a> and download your project or package when ready.</p>\n<h2 id=\"creer\">2. Create a counter</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Terminal · Windows or Linux",
        "source": "node overcrow.mjs init my-widget\ncd my-widget\nnpm run dev"
      },
      {
        "kind": "html",
        "html": "<p>Open the URL printed in the terminal and click +1. This browser preview reloads valid edits; an error leaves the last working version visible.</p>\n<h2 id=\"personnaliser\">3. Make a change</h2>\n<p>Change colors in <code>widget/styles.css</code> or labels in <code>widget/locales/en.mjs</code> and <code>widget/locales/fr.mjs</code>. <code>widget/index.html</code> controls the layout.</p>\n<p>To add two on each click, replace the existing <code>increment()</code> function in <code>widget/view.js</code> with:</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "widget/view.js · increment()",
        "source": "function increment() {\n  count += 2;\n  renderCount();\n}"
      },
      {
        "kind": "html",
        "html": "<p>Change the button label from +1 to +2 in <code>widget/index.html</code>.</p>\n<h2 id=\"exporter\">4. Build a package</h2>\n<p>Stop the preview with Ctrl+C, then run:</p>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "From my-widget/",
        "source": "npm run check\nnpm run package"
      },
      {
        "kind": "html",
        "html": "<p>The terminal prints the <code>.ocpkg</code> path under <code>.overcrow-output/</code>. It contains the files in <code>widget/</code>.</p>\n<h2 id=\"tester\">5. Test in OverCrow</h2>\n<p>Import the package in Control Center, review its permissions and activate it from the overlay library. In a game, test clicks, resizing and hiding/showing the widget.</p>\n<p>For an update, increase <code>version</code> in <code>widget/manifest.json</code>, then package and import again.</p>\n<h2 id=\"ensuite\">Next steps</h2>\n<p><a href=\"/docs/en/api-stockage/\">Save preferences</a>, <a href=\"/docs/en/api-reseau/\">call an API</a> or <a href=\"/docs/en/exemples/\">try another example</a>. <a href=\"/docs/en/kit-createur/\">The creator kit guide</a> covers preview controls, native testing and PowerShell troubleshooting.</p>"
      }
    ]
  },
  {
    "key": "installation",
    "slug": "installation",
    "group": "get-started",
    "label": "Installation",
    "title": "Installation",
    "description": "Get the creator kit, the SDK and OverCrow.",
    "headings": [
      {
        "id": "kit",
        "title": "Start with the kit"
      },
      {
        "id": "telechargements",
        "title": "Download the SDK"
      },
      {
        "id": "matrice",
        "title": "System requirements"
      },
      {
        "id": "obtenir-overcrow",
        "title": "Install OverCrow"
      },
      {
        "id": "import",
        "title": "Import the SDK"
      },
      {
        "id": "apercu",
        "title": "Serve a plain HTML project"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"kit\">Start with the kit</h2>\n<p>The <a href=\"/docs/en/kit-createur/\">creator kit</a> includes the SDK, templates and local tools. Install Node.js 22+ and follow <a href=\"/docs/en/premier-widget/\">Your first widget</a>.</p>\n<h2 id=\"telechargements\">Download the SDK</h2>\n<p>For an existing project, download <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/content/sdk/overcrow.js\" download>overcrow.js</a>, <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/content/sdk/overcrow.d.ts\" download>the TypeScript declarations</a> and <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/content/sdk/LICENSE\" download>the MIT license</a>. The <a href=\"/docs/en/exemples/\">example archives</a> already include them.</p>\n<p>Write JavaScript directly or compile TypeScript with your own tools. Ship the resulting static files; the .d.ts file supplies editor types.</p>\n<h2 id=\"matrice\">System requirements</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Task, Linux, Windows\"><table><thead><tr><th>Task</th><th>Linux</th><th>Windows</th></tr></thead><tbody><tr><td>Create, preview and package</td><td>Node.js 22+</td><td>Node.js 22+</td></tr><tr><td>Run a package in the overlay</td><td>Import in Control Center</td><td>Import in Control Center</td></tr><tr><td>Native development and devtools</td><td>Installed CLI and Control Center for the same user</td><td>Compatible OverCrow installation</td></tr><tr><td>Native development display</td><td>Offscreen; import a package to test in game</td><td>Same behavior</td></tr></tbody></table></div>\n<p><strong>Linux :</strong> Hyprland 0.55+ Wayland, Plasma 6 Wayland, GNOME 46–50 Wayland or X11/EWMH.<br/><strong>Windows :</strong> Windows 10 1809+ or Windows 11 x64, with WebView2 and the application’s graphics prerequisites.</p>\n<p>Use windowed or borderless games. Exclusive fullscreen is unsupported.</p>\n<h2 id=\"obtenir-overcrow\">Install OverCrow</h2>\n<p><a href=\"https://github.com/Valhallab/playervox-overcrow-releases/releases\" rel=\"noreferrer\">Download an official release</a>. On Linux, check that the native CLI is available:</p>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Linux terminal",
        "source": "command -v overcrow-widget"
      },
      {
        "kind": "html",
        "html": "<p>If the command is missing, check the installed OverCrow package.</p>\n<h2 id=\"import\">Import the SDK</h2><p>Add this markup to your view’s <code>index.html</code> before loading the module:</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<p id=\"status\" role=\"status\"></p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\nif (overcrow.runtime.role === 'unavailable') {\n  document.querySelector('#status').textContent = 'OverCrow services are not connected.';\n}"
      },
      {
        "kind": "html",
        "html": "<p>Load <code>view.js</code> with <code>&lt;script type=\"module\" src=\"./view.js\"&gt;&lt;/script&gt;</code>. Host services require OverCrow; the creator-kit browser preview simulates them.</p>\n<h2 id=\"apercu\">Serve a plain HTML project</h2>\n<p>For a simple layout preview, serve the folder with your editor’s local server. With Python 3 installed, use:</p>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Linux · optional static server",
        "source": "python3 -m http.server 4173 --bind 127.0.0.1"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Windows · Python launcher",
        "source": "py -m http.server 4173 --bind 127.0.0.1"
      },
      {
        "kind": "html",
        "html": "<p>Open <code>http://127.0.0.1:4173/</code>. Double-clicking index.html can block JavaScript modules on file:// URLs. This static server provides no host services.</p>\n<p><a href=\"/docs/en/api-services/\">See available host services</a>.</p>"
      }
    ]
  },
  {
    "key": "concepts",
    "slug": "concepts",
    "group": "get-started",
    "label": "Concepts",
    "title": "How a widget works",
    "description": "An HTML view, a manifest and an optional controller.",
    "headings": [
      {
        "id": "vue",
        "title": "The view"
      },
      {
        "id": "controleur",
        "title": "An optional controller"
      },
      {
        "id": "permissions",
        "title": "The manifest"
      },
      {
        "id": "cycle",
        "title": "From project to package"
      },
      {
        "id": "framework",
        "title": "Use a framework"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"vue\">The view</h2>\n<p>A widget starts with a local HTML page. Write its interface with HTML, CSS and JavaScript. OverCrow provides the frame, position and panel opacity. Keep the HTML/body background transparent and check small sizes.</p>\n<h2 id=\"controleur\">An optional controller</h2>\n<p>Add a second HTML document when requests or cached state should survive a native view reload. The view sends messages to this controller; the controller replies with data to display. Use storage for data that must survive a full restart.</p>\n<div class=\"architecture-strip\"><span>Controller · data</span><span aria-hidden=\"true\">⇄</span><span>runtime.send</span><span aria-hidden=\"true\">⇄</span><span>View · interface</span></div>\n<p><a href=\"/docs/en/api-runtime/\">See a counter with a controller</a>.</p>\n<h2 id=\"permissions\">The manifest</h2>\n<p>The manifest identifies your widget, names its files and declares permissions. OverCrow checks these permissions for network requests, storage, clipboard writes and game events. <a href=\"/docs/en/manifeste/\">Read the manifest guide</a>.</p>\n<h2 id=\"cycle\">From project to package</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Step, Result\"><table><thead><tr><th>Step</th><th>Result</th></tr></thead><tbody><tr><td>Develop</td><td>Edit files and preview your widget.</td></tr><tr><td>Prepare</td><td>Validate the manifest and calculate file hashes.</td></tr><tr><td>Distribute</td><td>Export a .ocpkg, test it in OverCrow and follow marketplace admission.</td></tr></tbody></table></div>\n<h2 id=\"framework\">Use a framework</h2>\n<p>Any framework that produces local static files can work. Run its build yourself and ship scripts, styles, images, fonts and licenses in the widget. WASM can handle local computation. Keep bundles small and pause unnecessary work when hidden.</p>\n<p>The runtime blocks remote images, iframes, Web Workers and audio/video media. A network permission enables <code>overcrow.fetch</code> requests only. Use SDK data or an authorized HTTPS API; game memory and native modules are outside the widget’s access.</p>"
      }
    ]
  },
  {
    "key": "creator-kit",
    "slug": "kit-createur",
    "group": "build",
    "label": "Creator kit",
    "title": "Creator kit",
    "description": "Create, preview and package a widget on Windows or Linux.",
    "headings": [
      {
        "id": "creer",
        "title": "Create a project"
      },
      {
        "id": "fichiers",
        "title": "Project files"
      },
      {
        "id": "commandes",
        "title": "Commands"
      },
      {
        "id": "apercu",
        "title": "Browser preview"
      },
      {
        "id": "fixtures",
        "title": "Simulate data"
      },
      {
        "id": "natif",
        "title": "Test the native engine"
      },
      {
        "id": "export",
        "title": "Import your package"
      },
      {
        "id": "diagnostics",
        "title": "Troubleshooting"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p>Install Node.js 22+, then extract the kit. <a href=\"https://github.com/Valhallab/playervox-overcrow-releases/tree/main/tools/creator-kit\" rel=\"noreferrer\">The source</a> is available on GitHub; the download is pinned to this documentation revision.</p>\n<p><a class=\"button-primary\" href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/creator-kit.zip\" download>Download the creator kit ↓</a> &nbsp; <a href=\"/studio/en/\">Or use the online Studio →</a></p>\n<h2 id=\"creer\">Create a project</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "From the extracted kit folder",
        "source": "node overcrow.mjs init my-widget --template counter --id com.example.my-widget\ncd my-widget\nnpm run dev"
      },
      {
        "kind": "html",
        "html": "<p>Choose <code>blank</code> for an empty view, <code>counter</code> to start or <code>checklist</code> for saved tasks. Omit <code>--id</code> to generate an ID. Existing folders are protected. <a href=\"/docs/en/premier-widget/\">Full tutorial</a>.</p>\n<h2 id=\"fichiers\">Project files</h2>"
      },
      {
        "kind": "code",
        "language": "text",
        "title": "Only widget/ is packaged",
        "source": "my-widget/\n  widget/             # HTML, CSS, JS, manifest and SDK\n  preview.json        # Simulated data\n  tooling/            # Local tools\n  package.json        # Development commands\n  .overcrow-output/   # Exported packages"
      },
      {
        "kind": "html",
        "html": "<p>Keep API keys, personal fixtures and build tools outside <code>widget/</code>. Place your framework’s compiled static output there.</p>\n<p>Edit Counter/Checklist labels in <code>widget/locales/en.mjs</code> and <code>fr.mjs</code>. <code>i18n.mjs</code> updates <code>data-i18n</code> elements and the page language; edit interactions in <code>view.js</code>.</p>\n<p>In Studio, select/reset templates in the header and manage files in the explorer. Update imports after renaming. Download the project to keep browser drafts elsewhere; the ZIP preserves empty folders.</p>\n<h2 id=\"commandes\">Commands</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Command, Result\"><table><thead><tr><th>Command</th><th>Result</th></tr></thead><tbody><tr><td>npm run dev</td><td>Browser preview; reloads valid edits.</td></tr><tr><td>npm run check</td><td>Validate sources and manifest without changes.</td></tr><tr><td>npm run package</td><td>Calculate hashes and export to .overcrow-output/.</td></tr><tr><td>npm run dev -- --native</td><td>Native session; keeps the controller during view reloads.</td></tr><tr><td>npm run doctor</td><td>Report Node, OS and native host availability.</td></tr></tbody></table></div>\n<p>Ctrl+C stops a session. For another preview port: <code>npm run dev -- --port 4176</code>.</p>\n<h2 id=\"apercu\">Browser preview</h2>\n<p>Open the printed 127.0.0.1 URL. Simulate the session, language and overlay mode. Invalid edits keep the last valid view; permission changes require a restart.</p>\n<ul><li>The three-dot menu adjusts content scale (50–175%) and frame opacity (0–100%); starters use 100%. Use sliders or enter a percentage. Widget CSS backgrounds stay independent.</li>\n<li>Drag the 22-pixel top strip to move. The starter’s lower-right handle resizes from its fixed upper-left corner. Arrow keys work when focused; Shift increases the step.</li>\n<li>The eye controls passive visibility. Passive mode hides frame controls and updates SDK visibility and <code>overlayMode</code>. Close is illustrative. Escape or an outside click closes options.</li>\n<li>Frame settings survive widget reloads until page refresh. OverCrow supplies this frame; keep it outside your widget.</li></ul>\n<p>Reload restarts view and controller. Legacy browser storage is separated by widget ID; use <code>getItem/setItem</code> instead of <code>localStorage.myKey</code>. <a href=\"/docs/en/api-stockage/\">SDK storage behavior</a>.</p>\n<h2 id=\"fixtures\">Simulate data</h2>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "preview.json · beside widget/",
        "source": "{\n  \"snapshot\": { \"running\": true, \"selectedActive\": true, \"sessionElapsedMs\": 300000 },\n  \"responses\": [{\n    \"url\": \"https://api.example.com/v2/items\", \"method\": \"GET\",\n    \"status\": 200, \"json\": { \"data\": [{ \"name\": \"Example\" }] }\n  }]\n}"
      },
      {
        "kind": "html",
        "html": "<p>Declare the matching HTTPS permission. Fixtures match exact method/URL, ignore request bodies and make no external request. Use <code>text</code> for text or status 503 for failure. Missing fixtures return <code>fixture_missing</code>. Named game events require replay.</p>\n<h2 id=\"natif\">Test the native engine</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Windows and Linux",
        "source": "npm run dev -- --native\nnpm run dev -- --native --devtools\nnpm run dev -- --native --replay ./events.json"
      },
      {
        "kind": "html",
        "html": "<p>Linux requires Control Center and <code>overcrow-widget</code>; Windows requires a compatible installation. For a portable host, add <code>--host \"C:/Tools/OverCrow/OverCrow.exe\"</code>.</p>\n<p>The kit recalculates hashes and reloads valid views while retaining the controller. Restart after controller edits, expanded permissions or storage permission changes.</p>\n<aside class=\"note note-info\"><strong>Offscreen session</strong><p>Use devtools to inspect the native engine. Import a package to test overlay input, focus, capture and clipboard gestures.</p></aside>\n<h2 id=\"export\">Import your package</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Create a .ocpkg",
        "source": "npm run package"
      },
      {
        "kind": "html",
        "html": "<p>Import the printed path in Control Center, review permissions and activate the widget. Increase <code>version</code> in <code>widget/manifest.json</code> for each update. Hash suffixes preserve exports; they do not change the version.</p>\n<h2 id=\"diagnostics\">Troubleshooting</h2>\n<p>If PowerShell blocks <code>npm.ps1</code>, use <code>npm.cmd run dev</code> or <code>node tooling/overcrow.mjs dev</code>.</p>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Structured diagnostics",
        "source": "node tooling/overcrow.mjs check --json"
      },
      {
        "kind": "html",
        "html": "<p>JSON reports <code>ok</code>, error code, field and action; failure exits nonzero. OverCrow and the marketplace perform final validation. <a href=\"/docs/en/depannage/\">Look up an error</a>.</p>\n<p><a href=\"/docs/en/api-services/\">Host services and widget examples</a>.</p>"
      }
    ]
  },
  {
    "key": "manifest",
    "slug": "manifeste",
    "group": "build",
    "label": "Manifest",
    "title": "The manifest",
    "description": "Identify your widget and declare its files and permissions.",
    "headings": [
      {
        "id": "minimal",
        "title": "Start with this manifest"
      },
      {
        "id": "champs",
        "title": "Fields"
      },
      {
        "id": "reseau",
        "title": "Allow an HTTPS request"
      },
      {
        "id": "fichiers",
        "title": "Include your files"
      },
      {
        "id": "limites",
        "title": "Limits"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"minimal\">Start with this manifest</h2>\n<p>This is enough for a local view. The kit generates file hashes when packaging.</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "widget/manifest.json · before packaging",
        "source": "{\n  \"schemaVersion\": 1,\n  \"id\": \"com.example.my-widget\",\n  \"version\": \"1.0.0\",\n  \"apiVersion\": \"1\",\n  \"entrypoints\": { \"view\": \"index.html\" },\n  \"permissions\": {\n    \"network\": [], \"gameEvents\": [],\n    \"storage\": false, \"clipboardWrite\": false\n  }\n}"
      },
      {
        "kind": "html",
        "html": "<p>Keep <code>id</code> stable across updates: it identifies the widget’s installation and storage.</p>\n<h2 id=\"champs\">Fields</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Field, Meaning\"><table><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td>schemaVersion</td><td>The number 1.</td></tr><tr><td>apiVersion</td><td>The string \"1\", or \"2\" for host services and presentation.</td></tr><tr><td>id</td><td>Stable lowercase reverse-domain ID, such as com.example.my-widget.</td></tr><tr><td>version</td><td>Widget SemVer, such as 1.0.0; increase it for updates.</td></tr><tr><td>entrypoints.view</td><td>Local HTML page to display.</td></tr><tr><td>entrypoints.controller</td><td>Optional local HTML controller.</td></tr><tr><td>permissions</td><td>The network, events, storage, clipboard or native capabilities you need.</td></tr><tr><td>localization</td><td>Languages you ship and the default locale.</td></tr><tr><td>presentation</td><td>API v2 sizing and native options.</td></tr><tr><td>files</td><td>Generated file sizes and SHA-256 hashes.</td></tr></tbody></table></div>\n<p><a href=\"/docs/en/api-services/\">See API v2 host services</a> and <a href=\"/docs/en/api-langue/\">language declarations</a>.</p>\n<h2 id=\"reseau\">Allow an HTTPS request</h2>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "Inside permissions",
        "source": "\"network\": [{\n  \"origin\": \"https://api.example.com\",\n  \"method\": \"GET\",\n  \"pathPrefix\": \"/v2/\"\n}]"
      },
      {
        "kind": "html",
        "html": "<p>Call <code>overcrow.fetch</code> with the declared origin, method and path. A trailing slash permits the path’s subtree; without it, only the exact path is allowed. Host wildcards, redirects and local destinations are rejected. <a href=\"/docs/en/api-reseau/\">Network reference</a>.</p>\n<h2 id=\"fichiers\">Include your files</h2>\n<p>Put HTML, CSS, JavaScript, images, fonts, translations and licenses in <code>widget/</code>. Use relative ASCII paths, with no <code>..</code>, links or case collisions. Keep build tools outside this folder.</p>\n<p>The generated <code>files</code> must match every packaged file; the manifest excludes its own hash. The native CLI requires this ledger for <code>dev</code> and <code>package</code>. Unknown fields are rejected.</p>\n<p>Review added permissions and restart development to accept them. Preparation never infers permissions from your code.</p>\n<h2 id=\"limites\">Limits</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Item, Limit\"><table><thead><tr><th>Item</th><th>Limit</th></tr></thead><tbody><tr><td>Package</td><td>128 MiB; 4,096 entries including the manifest</td></tr><tr><td>Manifest</td><td>1 MiB</td></tr><tr><td>Paths</td><td>192 ASCII bytes</td></tr></tbody></table></div>\n<p>See the relevant API pages for <a href=\"/docs/en/api-reseau/\">request limits</a>, <a href=\"/docs/en/api-runtime/\">messages</a> and <a href=\"/docs/en/api-langue/\">languages</a>.</p>"
      }
    ]
  },
  {
    "key": "examples",
    "slug": "exemples",
    "group": "build",
    "label": "Examples",
    "title": "Examples",
    "description": "Download a starting project, a host-service example or a standalone widget.",
    "headings": [
      {
        "id": "blank",
        "title": "Blank · empty view"
      },
      {
        "id": "compteur",
        "title": "Counter · first widget"
      },
      {
        "id": "checklist",
        "title": "Checklist · saved tasks"
      },
      {
        "id": "marche",
        "title": "Warframe Market · API and cache"
      },
      {
        "id": "utilisation",
        "title": "Use a source archive"
      },
      {
        "id": "references-v2",
        "title": "More widget examples"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"blank\">Blank · empty view</h2><p>HTML, CSS, JavaScript and manifest, with the SDK and license. Add your own interface.</p><p><a class=\"button-primary\" href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/blank.zip\" download>Download sources ↓</a> &nbsp; <a href=\"/studio/en/\">Edit in Studio →</a></p><h2 id=\"compteur\">Counter · first widget</h2><p>A counter with two buttons. Learn layout and packaging with a view that uses no network or storage.</p><p><a class=\"button-primary\" href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/counter.zip\" download>Download sources ↓</a> &nbsp; <a href=\"/studio/en/\">Edit in Studio →</a></p><h2 id=\"checklist\">Checklist · saved tasks</h2><p>Add, check and remove tasks. Data is versioned and validated; invalid data is reported and preserved. Requests <code>storage: true</code>.</p><p><a class=\"button-primary\" href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/checklist.zip\" download>Download sources ↓</a> &nbsp; <a href=\"/studio/en/\">Edit in Studio →</a></p><h2 id=\"marche\">Warframe Market · API and cache</h2><p>Search a catalog, list buy/sell offers and copy a message. A controller manages requests and an IndexedDB cache. Permissions: GET to <code>https://api.warframe.market/v2/</code>, storage and clipboard writes.</p><p><a class=\"button-primary\" href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/warframe-market.zip\" download>Download sources ↓</a></p><p>Warframe Market is reference bundle 2.0.5, including its bundled SDK. <a href=\"https://github.com/Valhallab/playervox-overcrow-releases/tree/main/widgets/warframe-market\" rel=\"noreferrer\">Source history ↗</a>.</p>\n<h2 id=\"utilisation\">Use a source archive</h2>\n<p>Create a <a href=\"/docs/en/kit-createur/\">creator-kit project</a>, then copy the extracted files into its <code>widget/</code>. Choose your own ID and version, and keep MIT notices. You can also use the <a href=\"/docs/en/developpement/\">Linux native CLI</a>.</p>\n<p>These archives are sources. Package and import them to test rendering and interaction in OverCrow. Counter and Checklist are starter projects, not marketplace listings.</p>"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"references-v2\">More widget examples</h2><p>The examples include English and French labels. Session and Clock use the game context and JavaScript time; Performance, FPS and Media use <a href=\"/docs/en/api-services/\">host services</a>. Notes, Stopwatch and Journal own their data in <a href=\"/docs/en/api-stockage/\">widget storage</a>. Score calls the <a href=\"/docs/en/api-playervox/\">public PlayerVox API</a>.</p>"
      },
      {
        "kind": "html",
        "html": "<p><a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/session.zip\">session</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/clock.zip\">clock</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/performance.zip\">performance</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/fps.zip\">fps</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/media.zip\">media</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/stopwatch.zip\">stopwatch</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/notes.zip\">notes</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/journal.zip\">journal</a> · <a href=\"https://raw.githubusercontent.com/Valhallab/playervox-overcrow-releases/main/published/docs/downloads/score.zip\">score</a></p>"
      }
    ]
  },
  {
    "key": "stopwatch",
    "slug": "api-stopwatch",
    "group": "build",
    "label": "Stopwatch",
    "title": "Make a stopwatch",
    "description": "Build a stopwatch with JavaScript. No native capability is required.",
    "headings": [
      {
        "id": "permissions",
        "title": "Storage permission"
      },
      {
        "id": "example",
        "title": "Start and pause"
      },
      {
        "id": "control",
        "title": "How it works"
      },
      {
        "id": "data-and-controls",
        "title": "Saving and migration"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">A stopwatch only needs JavaScript. Its state belongs to your widget.</p><h2 id=\"permissions\">Permissions</h2><p>No SDK permission is needed for this example.</p>"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"example\">Start and pause</h2><p>Create these two files in your widget.</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<output id=\"time\">0.0 s</output>\n<button id=\"toggle\" type=\"button\">Start</button>\n<script type=\"module\" src=\"./view.js\"></script>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "const time = document.querySelector('#time');\nconst toggle = document.querySelector('#toggle');\nlet elapsedMs = 0, startedAt = null;\ntoggle.onclick = () => {\n  if (startedAt === null) startedAt = performance.now();\n  else {\n    elapsedMs += performance.now() - startedAt;\n    startedAt = null;\n  }\n  toggle.textContent = startedAt === null ? 'Start' : 'Pause';\n};\nconst timer = setInterval(() => {\n  const current = elapsedMs + (startedAt === null ? 0 : performance.now() - startedAt);\n  time.textContent = `${(current / 1000).toFixed(1)} s`;\n}, 100);\nwindow.addEventListener('pagehide', () => clearInterval(timer), { once: true });"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"control\">How it works</h2><p><code>performance.now()</code> measures elapsed time. The interval only refreshes the display; reloading resets the timer.</p><h2 id=\"data-and-controls\">Going further</h2><p>For persistence, use <a href=\"/docs/en/api-stockage/\">your widget’s storage</a>. For the active game’s duration, read <a href=\"/docs/en/api-jeu/\">sessionElapsedMs</a>. The built-in stopwatch remains private; remove old stopwatch capabilities and SDK calls.</p>"
      }
    ]
  },
  {
    "key": "notes",
    "slug": "api-notes",
    "group": "build",
    "label": "Notes",
    "title": "Save a note",
    "description": "Build a small editor and save its text in your widget’s isolated storage.",
    "headings": [
      {
        "id": "permissions",
        "title": "Storage permission"
      },
      {
        "id": "example",
        "title": "Minimal editor"
      },
      {
        "id": "actions-and-saving",
        "title": "Saving and migration"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">A note is a textarea, a Save button and a value in <code>overcrow.storage</code>. This widget owns its note; built-in OverCrow notes are not exposed.</p><h2 id=\"permissions\">Storage permission</h2><p>Merge this field into your existing manifest. It requests persistent widget storage; the browser preview always uses temporary memory.</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json · fields to merge",
        "source": "{\n  \"permissions\": { \"storage\": true }\n}"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"example\">Minimal editor</h2><p>Put <code>overcrow.js</code> and <code>view.js</code> beside <code>index.html</code>. Use this markup, then the JavaScript below.</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<textarea id=\"note\" aria-label=\"Note\" maxlength=\"8000\" disabled></textarea>\n<button id=\"save\" type=\"button\" disabled>Save</button>\n<p id=\"status\" role=\"status\"></p>\n<script type=\"module\" src=\"./view.js\"></script>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\nconst note = document.querySelector('#note');\nconst save = document.querySelector('#save');\nconst status = document.querySelector('#status');\ntry {\n  const stored = await overcrow.storage.get('note');\n  if (stored !== undefined && typeof stored !== 'string') throw new Error('Invalid note');\n  note.value = stored ?? '';\n  note.disabled = save.disabled = false;\n} catch { status.textContent = 'Could not load the note.'; }\nsave.onclick = async () => {\n  note.disabled = save.disabled = true;\n  try {\n    await overcrow.storage.set('note', note.value);\n    status.textContent = 'Saved';\n  } catch { status.textContent = 'Save failed. Your text is still here.'; }\n  finally { note.disabled = save.disabled = false; }\n};"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"actions-and-saving\">Saving and migration</h2><p>The button saves only this widget’s text. A failed write leaves it in the editor. Check <code>overcrow.storage.getInfo()</code> to distinguish <code>persistent</code> from <code>temporary</code>; see the <a href=\"/docs/en/api-stockage/\">storage guide</a> for limits and error handling.</p><p>For a checklist, save an array of your own items. The <a href=\"/docs/en/exemples/#checklist\">Checklist starter</a> provides a complete example. Remove the old notes capabilities and calls from existing manifests and code; there is no SDK access to the built-in note list or editor.</p>"
      }
    ]
  },
  {
    "key": "playervox",
    "slug": "api-playervox",
    "group": "build",
    "label": "Public score",
    "title": "Read a public PlayerVox score",
    "description": "Fetch a community score through overcrow.fetch without an account or native integration.",
    "headings": [
      {
        "id": "permissions",
        "title": "Network permission"
      },
      {
        "id": "example",
        "title": "Show the Portal 2 score"
      },
      {
        "id": "ratings-and-reviews",
        "title": "Accounts and migration"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">The public PlayerVox score endpoint accepts an anonymous GET request. Use <code>overcrow.fetch</code> as with any supported HTTPS API; no PlayerVox account, token or native SDK service is involved.</p><h2 id=\"permissions\">Network permission</h2><p>Merge this permission into your manifest. It allows only the score URL for Steam app 620 (Portal 2).</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json · fields to merge",
        "source": "{\n  \"permissions\": {\n    \"network\": [{\n      \"origin\": \"https://api.playervox.com\",\n      \"method\": \"GET\",\n      \"pathPrefix\": \"/api/v1/overcrow/games/steam/620/score\"\n    }]\n  }\n}"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"example\">Show the Portal 2 score</h2><p>Load this module from your view with <code>&lt;script type=\"module\" src=\"./view.js\"&gt;&lt;/script&gt;</code>, with <code>overcrow.js</code> beside it. The module creates its own text output.</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\n\nconst output = document.createElement('p');\noutput.textContent = 'Loading…';\ndocument.body.append(output);\ntry {\n  const response = await overcrow.fetch(\n    'https://api.playervox.com/api/v1/overcrow/games/steam/620/score'\n  );\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  const data = await response.json();\n  if (typeof data?.game?.name !== 'string' ||\n      (data.score !== null && (!Number.isFinite(data.score) || data.score < 0 || data.score > 100))) {\n    throw new Error('Invalid score');\n  }\n  output.textContent = data.score === null\n    ? `${data.game.name} · No ratings yet`\n    : `${data.game.name} · ${data.score.toFixed(1)}/100`;\n} catch {\n  output.textContent = 'Score unavailable. Try again later.';\n}"
      },
      {
        "kind": "html",
        "html": "<p>The score uses a 0–100 scale; <code>null</code> means no rating is available. To show another game, change both the URL and its manifest permission. The browser preview needs a matching response fixture. See the <a href=\"/docs/en/api-reseau/\">network guide</a> for HTTP errors and request limits.</p><h2 id=\"ratings-and-reviews\">Accounts and migration</h2><p>Remove the old PlayerVox capabilities and SDK calls. Public widgets cannot use OverCrow’s PlayerVox login, personal ratings, followed players, reviews or native editors. The public score request reads no connected-account state.</p>"
      }
    ]
  },
  {
    "key": "journal",
    "slug": "api-journal",
    "group": "build",
    "label": "Session log",
    "title": "Keep a session log",
    "description": "Save a small history from the current game context in your own widget storage.",
    "headings": [
      {
        "id": "permissions",
        "title": "Storage permission"
      },
      {
        "id": "example",
        "title": "Save the current session"
      },
      {
        "id": "pages-and-actions",
        "title": "History and migration"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">A widget can record the current game context in its own storage. It cannot read, edit or delete OverCrow’s built-in local or cloud journal.</p><h2 id=\"permissions\">Storage permission</h2><p>Merge this field into your existing manifest. It requests persistent widget storage; the browser preview always uses temporary memory.</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json · fields to merge",
        "source": "{\n  \"permissions\": { \"storage\": true }\n}"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"example\">Save the current session</h2><p>Put <code>overcrow.js</code> and <code>view.js</code> beside <code>index.html</code>. Use this markup, then the JavaScript below.</p><p>Each click saves a timestamp and the current elapsed minutes. This example keeps the latest 20 entries.</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<button id=\"save\" type=\"button\">Save current session</button>\n<pre id=\"entries\"></pre>\n<p id=\"status\" role=\"status\"></p>\n<script type=\"module\" src=\"./view.js\"></script>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\nconst save = document.querySelector('#save');\nconst output = document.querySelector('#entries');\nconst status = document.querySelector('#status');\nsave.onclick = async () => {\n  save.disabled = true;\n  try {\n    const game = await overcrow.game.snapshot();\n    if (!game.selectedActive || game.sessionElapsedMs == null) throw new Error('No session');\n    const entries = await overcrow.storage.get('sessions') ?? [];\n    if (!Array.isArray(entries) || entries.length > 20 || entries.some(x => typeof x !== 'string')) {\n      throw new Error('Invalid session log');\n    }\n    const entry = `${new Date().toISOString()} · ${Math.floor(game.sessionElapsedMs / 60000)} min`;\n    const next = [...entries, entry].slice(-20);\n    await overcrow.storage.set('sessions', next);\n    output.textContent = next.join('\\n');\n    status.textContent = 'Saved';\n  } catch { status.textContent = 'Could not save this session.'; }\n  finally { save.disabled = false; }\n};"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"pages-and-actions\">History and migration</h2><p>This is a log of button clicks, not an automatic record of completed games. Add your own fields, pagination or delete controls as needed. Only overwrite stored data after validating its shape; keep the last displayed entries if a write fails.</p><p>Remove the old journal capabilities and calls. No native history or account data is copied into widget storage. The <a href=\"/docs/en/api-jeu/\">game context</a> supplies only the current session; the <a href=\"/docs/en/api-stockage/\">storage guide</a> covers limits and persistence.</p>"
      }
    ]
  },
  {
    "key": "network",
    "slug": "api-reseau",
    "group": "sdk-reference",
    "label": "Network",
    "title": "Network requests",
    "description": "Read an HTTPS API with overcrow.fetch.",
    "headings": [
      {
        "id": "signature",
        "title": "Call overcrow.fetch"
      },
      {
        "id": "exemple",
        "title": "Read a JSON response"
      },
      {
        "id": "reponse",
        "title": "Read the result"
      },
      {
        "id": "contraintes",
        "title": "Limits and permissions"
      },
      {
        "id": "recette",
        "title": "Handle failed loads"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"signature\">Call overcrow.fetch</h2>\n<p>Pass a URL and optional request settings. The default method is GET.</p>"
      },
      {
        "kind": "code",
        "language": "typescript",
        "title": "Supported signature",
        "source": "import type { OvercrowResponse } from './overcrow.js';\ntype Fetch = (url: string, options?: {\n  method?: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';\n  body?: string | ArrayBuffer | ArrayBufferView | null;\n}) => Promise<OvercrowResponse>;"
      },
      {
        "kind": "html",
        "html": "<p>Declare the HTTPS origin, method and path prefix in <a href=\"/docs/en/manifeste/\">the manifest</a>.</p>\n<h2 id=\"exemple\">Read a JSON response</h2><p>Add this markup to your view’s <code>index.html</code> before loading the module:</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<p id=\"status\" role=\"status\"></p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js · requires GET /v2/ permission",
        "source": "import overcrow from './overcrow.js';\nconst status = document.querySelector('#status');\ntry {\n  status.textContent = 'Loading…';\n  const response = await overcrow.fetch('https://api.warframe.market/v2/items');\n  if (!response.ok) throw new Error('HTTP ' + response.status);\n  const payload = await response.json();\n  if (!Array.isArray(payload?.data)) throw new Error('Invalid catalog');\n  status.textContent = payload.data.length + ' items available';\n} catch (error) {\n  status.textContent = 'Catalog unavailable. Try again.';\n  console.error(error.code ?? error.message);\n}"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"reponse\">Read the result</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Member, Behavior\"><table><thead><tr><th>Member</th><th>Behavior</th></tr></thead><tbody><tr><td>status / ok</td><td>HTTP status; ok is true for 200–299.</td></tr><tr><td>contentType</td><td>Content type or null.</td></tr><tr><td>bodyUsed</td><td>True after the first body read.</td></tr><tr><td>json()</td><td>Parse the body once; validate the returned data before using it.</td></tr><tr><td>text() / arrayBuffer()</td><td>Read text or bytes once. A second body read rejects with body_used.</td></tr></tbody></table></div>\n<p>A 404 or 503 is an HTTP response: check <code>response.ok</code>. A denied permission or timeout rejects the call with an SDK error.</p>\n<h2 id=\"contraintes\">Limits and permissions</h2>\n<p>The host limits requests to 30 seconds, 2 MiB for request bodies and 32 MiB for response bodies. It rejects redirects, proxies and local destinations. Bodies are sent as bytes; a string does not automatically set a JSON Content-Type.</p>\n<p><code>headers</code>, <code>signal</code>, cookies and ambient authentication are unsupported. A <code>Promise.race</code> can stop your wait, but does not cancel the native request. Ignore replies from superseded searches.</p>\n<p>Use <code>overcrow.fetch</code> for external requests. This permission does not enable remote images, WebSockets, workers or iframes. Keep API keys out of distributed files; use a service you control for private credentials.</p>\n<h2 id=\"recette\">Handle failed loads</h2>\n<p>Keep the last valid data during refresh, distinguish empty results from errors, show cache age and offer a retry. <a href=\"/docs/en/exemples/\">Warframe Market</a> shows this with a controller and cache. <a href=\"/docs/en/depannage/\">Look up error codes</a>.</p>"
      }
    ]
  },
  {
    "key": "game",
    "slug": "api-jeu",
    "group": "sdk-reference",
    "label": "Game session",
    "title": "Game session",
    "description": "Read session state and follow its changes.",
    "headings": [
      {
        "id": "snapshot",
        "title": "Read the current session"
      },
      {
        "id": "champs",
        "title": "Snapshot fields"
      },
      {
        "id": "abonnement",
        "title": "Follow changes"
      },
      {
        "id": "evenements",
        "title": "Listen for named events"
      },
      {
        "id": "replay",
        "title": "Test with a fixture"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"snapshot\">Read the current session</h2><p>Add this markup to your view’s <code>index.html</code> before loading the module:</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<p id=\"duration\">—</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Read once",
        "source": "import overcrow from './overcrow.js';\nconst snapshot = await overcrow.game.snapshot();\nconst ms = snapshot.sessionElapsedMs;\ndocument.querySelector('#duration').textContent =\n  typeof ms === 'number' ? Math.floor(ms / 60000) + ' min' : 'Session unavailable';"
      },
      {
        "kind": "html",
        "html": "<p>A snapshot contains the session data available from OverCrow. A missing game or sensor can leave values unavailable. Check the type before formatting; zero is a valid measurement.</p>\n<h2 id=\"champs\">Snapshot fields</h2>\n<div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Field, Type / unit\"><table><thead><tr><th>Field</th><th>Type / unit</th></tr></thead><tbody><tr><td>running / selectedActive</td><td>Game/session activity booleans. A minimal snapshot may contain only running.</td></tr><tr><td>steamAppId</td><td>Number or null; no title or artwork.</td></tr><tr><td>sessionElapsedMs</td><td>Session duration in milliseconds, or null.</td></tr><tr><td>overlayMode</td><td>\"passive\" or \"interactive\".</td></tr><tr><td>cpuPercentHundredths</td><td>Process CPU in hundredths of a percent, or null.</td></tr><tr><td>residentBytes</td><td>Resident memory in bytes, or null.</td></tr><tr><td>cpuTemperatureMillicelsius / gpuTemperatureMillicelsius</td><td>Thousandths of a Celsius degree, or null.</td></tr></tbody></table></div>\n<p><code>GameSnapshot</code> describes these optional fields. Extra JSON fields are preserved; an invalid type in a known field rejects with <code>invalid_response</code>.</p>\n<p>This snapshot has no FPS, inventory, health or player-position field. <a href=\"/docs/en/api-services/\">Use host services</a> for features such as FPS, subject to host support.</p>\n<h2 id=\"abonnement\">Follow changes</h2>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Subscribe, then read the initial state",
        "source": "import overcrow from './overcrow.js';\nfunction render(snapshot) {\n  const ms = snapshot.sessionElapsedMs;\n  document.querySelector('#duration').textContent =\n    typeof ms === 'number' ? Math.floor(ms / 60000) + ' min' : 'Session unavailable';\n}\nconst stop = overcrow.game.onSnapshot(render);\nrender(await overcrow.game.snapshot());\nwindow.addEventListener('pagehide', stop, { once: true });"
      },
      {
        "kind": "html",
        "html": "<p><code>onSnapshot</code> delivers future changes. Read <code>snapshot()</code> for the initial state. Malformed events are ignored. The returned <code>stop()</code> function unsubscribes and is safe to call again.</p>\n<h2 id=\"evenements\">Listen for named events</h2>\n<p>For an integration or replay that emits named events, declare them in <code>permissions.gameEvents</code>, then subscribe:</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Example replay event",
        "source": "import overcrow from './overcrow.js';\n\nconst stop = overcrow.game.on('overcrow.game.session.v1', payload => {\n  // Validate the schema defined by the event or fixture.\n  console.log(payload);\n});\nwindow.addEventListener('pagehide', stop, { once: true });"
      },
      {
        "kind": "html",
        "html": "<p>The host or fixture must actually emit this name. Native snapshot changes use <code>onSnapshot</code>. A replay field such as <code>elapsedMs</code> is separate from the native <code>sessionElapsedMs</code>.</p>\n<h2 id=\"replay\">Test with a fixture</h2>\n<p><a href=\"/docs/en/developpement/\">Replay ordered events</a> to test your handlers without a game. Use OverCrow for actual game and sensor availability.</p>"
      },
      {
        "kind": "html",
        "html": "<p><a href=\"/docs/en/api-services/\">Explore other host services</a>.</p>"
      }
    ]
  },
  {
    "key": "services",
    "slug": "api-services",
    "group": "sdk-reference",
    "label": "Host services",
    "title": "OverCrow host services",
    "description": "Read performance and media data, control playback and size your widget.",
    "headings": [
      {
        "id": "permissions",
        "title": "1. Declare permissions"
      },
      {
        "id": "abonnements",
        "title": "2. Display live data"
      },
      {
        "id": "disponibilite",
        "title": "Handle unavailable data"
      },
      {
        "id": "donnees",
        "title": "Find fields and units"
      },
      {
        "id": "actions",
        "title": "Media controls"
      },
      {
        "id": "presentation",
        "title": "Size and display options"
      },
      {
        "id": "confidentialite",
        "title": "Widget and application data"
      },
      {
        "id": "simulation",
        "title": "Try an example"
      },
      {
        "id": "migration",
        "title": "Update an existing widget"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p>The host supplies <a href=\"/docs/en/api-performance/\">telemetry and FPS</a>, <a href=\"/docs/en/api-media/\">media information and playback controls</a>, and presentation settings. Your widget implements its own interface and features.</p><p>Other runtime tools cover <a href=\"/docs/en/api-jeu/\">current game context</a>, <a href=\"/docs/en/api-stockage/\">storage guide</a>, <a href=\"/docs/en/api-reseau/\">network guide</a>, <a href=\"/docs/en/api-presse-papiers/\">clipboard writes</a> and <a href=\"/docs/en/api-runtime/\">lifecycle and messages</a>.</p><h2 id=\"permissions\">1. Declare permissions</h2><p>Use <code>apiVersion: \"2\"</code> for host services. The available capabilities are <code>telemetry.read</code>, <code>fps.read</code>, <code>media.read</code> and <code>media.control</code>. Merge only the ones you need into your manifest:</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json · fields to merge",
        "source": "{\n  \"apiVersion\": \"2\",\n  \"permissions\": { \"capabilities\": [\"fps.read\"] }\n}"
      },
      {
        "kind": "html",
        "html": "<p>OverCrow asks the user to approve these permissions. A new permission needs new consent.</p><h2 id=\"abonnements\">2. Display live data</h2><p>For telemetry, FPS, media and presentation, <code>onSnapshot()</code> delivers the current state and later updates. This module creates an FPS label; put <code>overcrow.js</code> beside it and load it with <code>&lt;script type=\"module\" src=\"./view.js\"&gt;&lt;/script&gt;</code>.</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\n\nconst output = document.createElement('output');\ndocument.body.append(output);\nconst stop = overcrow.fps.onSnapshot(({ status, data }) => {\n  output.textContent = status === 'ready' && !data.stale && data.value != null\n    ? `${data.value} FPS` : '— FPS';\n});\nwindow.addEventListener('pagehide', stop, { once: true });"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"disponibilite\">Handle unavailable data</h2><p>A service snapshot contains <code>{ status, data, contextId, revision }</code>. Only <code>ready</code> and <code>stale</code> can include data; individual measurements may still be <code>null</code>.</p><table><thead><tr><th>Status</th><th>What to show</th></tr></thead><tbody><tr><td><code>ready</code></td><td>Current data.</td></tr><tr><td><code>stale</code></td><td>Older data and its age, <code>sampleAgeMs</code>.</td></tr><tr><td><code>unavailable</code> / <code>notConnected</code></td><td>A placeholder for a missing source.</td></tr><tr><td><code>unsupported</code></td><td>This host or platform lacks the feature.</td></tr><tr><td><code>permissionDenied</code></td><td>A missing permission notice.</td></tr><tr><td><code>rateLimited</code></td><td>Wait before retrying; use <code>retryAfterMs</code> when supplied.</td></tr></tbody></table><p>For a one-off read, use <code>snapshot()</code> and catch errors. <code>overcrow.host.capabilities()</code> reports <code>supported</code> and <code>granted</code>. Without a host or simulator, a read rejects with <code>bridge_unavailable</code>; an initial subscription failure reports <code>unavailable</code>.</p><p>Keep at most 128 active subscriptions and 32 pending actions. Unsubscribe when the view is disposed. Ignore replies from an old context.</p><h2 id=\"donnees\">Find fields and units</h2><p>The service pages describe fields, units and limits; the <a href=\"/docs/downloads/overcrow.d.ts\" download>TypeScript definitions</a> give the complete types. Treat missing measurements as unavailable.</p><h2 id=\"actions\">Media controls</h2><p>Call <code>overcrow.media.previous()</code>, <code>playPause()</code> or <code>next()</code> directly from a click in an interactive view. The host checks the permission and a short-lived, single-use user gesture. A result of <code>accepted</code> means queued; follow media snapshots for the resulting playback state.</p><p>Handle errors such as <code>permission_denied</code>, <code>unsupported_operation</code>, <code>stale_context</code> and <code>rate_limited</code>. Let the user retry after refreshing the state.</p><h2 id=\"presentation\">Size and display options</h2><table><thead><tr><th><code>presentation.sizing.mode</code></th><th>Behavior</th></tr></thead><tbody><tr><td><code>intrinsic</code></td><td>Fits the content.</td></tr><tr><td><code>autoHeight</code></td><td>Automatic height, adjustable width.</td></tr><tr><td><code>manual</code></td><td>Both dimensions can be resized.</td></tr></tbody></table><p>Declare integer <code>preferred</code>, <code>min</code> and <code>max</code> dimensions in the manifest. Up to 16 <code>boolean</code>, <code>enum</code> or bounded <code>number</code> options can appear in the host menu, with English and French labels. Read values through <code>overcrow.presentation.onSnapshot()</code>, in <code>data.options</code>.</p><p>From a view, report your content’s preferred unscaled CSS size, with integers from 1 to 4096. For example, a 240 × 80 widget:</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\n\nawait overcrow.presentation.reportSize({ width: 240, height: 80 });"
      },
      {
        "kind": "html",
        "html": "<p><code>reportSize()</code> and media controls return <code>{ status: \"accepted\" }</code> on success; failures reject. Size reporting is available only to views.</p><p>Group repeated size changes before reporting them. OverCrow owns frame position, resize, opacity, scale and passive visibility. See the <a href=\"/docs/en/exemples/#references-v2\">reference manifests</a> for complete options.</p><h2 id=\"confidentialite\">Widget and application data</h2><p>Widget storage is isolated from other widgets and built-in application data. The SDK exposes no built-in notes, stopwatch state, session history or connected PlayerVox/Twitch accounts.</p><p>Media permissions are sensitive: they cannot be combined with outbound network or clipboard writes, and they force temporary storage even with <code>storage: true</code>. Artwork is available only through <code>overcrow.assets.read(handle)</code> for a handle supplied by the media service. See the <a href=\"/docs/en/api-media/\">media guide</a> and <a href=\"/docs/en/api-stockage/\">storage guide</a>.</p><h2 id=\"simulation\">Try an example</h2><p>The <a href=\"/docs/en/exemples/#references-v2\">reference widgets</a> include performance, FPS and media displays. Browser previews use fictional service data; media controls change only that data. FPS defaults to <code>unsupported</code>. Test actual availability and permissions in OverCrow.</p><h2 id=\"migration\">Update an existing widget</h2><p>Replace <code>overcrow.js</code> and <code>overcrow.d.ts</code> together. API v1 remains supported; use <code>apiVersion: \"2\"</code> for capabilities or presentation. <code>game.onSnapshot()</code> still emits future updates only, and its original CPU units remain unchanged.</p><p>Remove withdrawn built-in service capabilities and calls from older API v2 widgets. Implement <a href=\"/docs/en/api-notes/\">notes</a>, a <a href=\"/docs/en/api-stopwatch/\">stopwatch</a> or a <a href=\"/docs/en/api-journal/\">session log</a> with JavaScript and widget storage. Read a <a href=\"/docs/en/api-playervox/\">public PlayerVox score</a> through the network API. The <a href=\"/docs/en/api-twitch/\">Twitch migration note</a> explains the removed integration.</p>"
      }
    ]
  },
  {
    "key": "performance",
    "slug": "api-performance",
    "group": "sdk-reference",
    "label": "Performance & FPS",
    "title": "Performance and FPS",
    "description": "Display available game measurements, with their units and freshness.",
    "headings": [
      {
        "id": "permissions",
        "title": "Permissions"
      },
      {
        "id": "example",
        "title": "Minimal example"
      },
      {
        "id": "data-and-limits",
        "title": "Units and availability"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">Build a compact performance display with telemetry for CPU, memory and temperatures, plus a separate FPS service. A missing measurement is not zero.</p><h2 id=\"permissions\">Permissions</h2><p>Merge these fields into your manifest. Use <code>telemetry.read</code> for system measurements and <code>fps.read</code> for frame rate; omit either permission if unused. Declaring them requests user consent and does not guarantee platform support.</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json (fragment)",
        "source": "{\n  \"apiVersion\": \"2\",\n  \"permissions\": {\n    \"network\": [],\n    \"clipboardWrite\": false,\n    \"capabilities\": [\n      \"telemetry.read\",\n      \"fps.read\"\n    ]\n  }\n}"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"example\">Minimal example</h2><p>Load this as a module in your view, with <code>overcrow.js</code> beside it. It creates two text labels. Each subscription reads the current value and then follows updates.</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\n\nconst cpu = document.createElement('p');\nconst fps = document.createElement('p');\ndocument.body.append(cpu, fps);\n\nfunction show(element, snapshot, value, stale, age) {\n  const message = value ?? `Measurement unavailable (${snapshot.status})`;\n  element.textContent = message + (stale ? ` · stale (${age ?? 'unknown'} ms old)` : '');\n}\n\nconst stopCpu = overcrow.telemetry.onSnapshot(snapshot => {\n  const value = snapshot.data?.normalizedCpuPercentHundredths;\n  show(cpu, snapshot, value == null ? null : `${value / 100}% CPU`,\n    snapshot.status === 'stale', snapshot.sampleAgeMs);\n});\nconst stopFps = overcrow.fps.onSnapshot(snapshot => {\n  const data = snapshot.data;\n  show(fps, snapshot, data?.value == null ? null : `${data.value} FPS`,\n    snapshot.status === 'stale' || data?.stale === true,\n    data?.sampleAgeMs ?? snapshot.sampleAgeMs);\n});\nwindow.addEventListener('pagehide', () => { stopCpu(); stopFps(); }, {once: true});"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"data-and-limits\">Units and availability</h2><table><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>normalizedCpuPercentHundredths</code></td><td>0–10000; divide by 100 for total-machine CPU percent.</td></tr><tr><td><code>residentBytes</code></td><td>Bytes; divide by 1048576 for MiB.</td></tr><tr><td><code>cpuTemperatureMillicelsius</code>, <code>gpuTemperatureMillicelsius</code></td><td>Host sensor temperatures; divide by 1000 for °C.</td></tr><tr><td>FPS <code>value</code>, <code>sampleAgeMs</code>, <code>stale</code></td><td>Frame rate, measurement age in milliseconds, and freshness.</td></tr></tbody></table><p>Every measurement can be null, even when the service is <code>ready</code>. Keep a stale label and its age when showing old data; an absent age is unknown. A service marked <code>unsupported</code> has no provider on that host.</p><p>Linux CPU and memory cover the selected Steam process group or manually selected process descendants. Windows uses the validated target process. Temperatures describe host sensors, not the game process. Windows CPU temperature is unavailable; Windows FPS needs the validated capture service. Native Wayland currently has no FPS source.</p><p>The API v1 <code>game.cpuPercentHundredths</code> field keeps its original meaning; do not treat it as normalized CPU. Browser preview measurements are fictional and FPS defaults to unsupported.</p><p>Continue with <a href=\"/docs/en/api-services/#donnees\">shared service rules</a>, the <a href=\"/docs/downloads/overcrow.d.ts\">complete TypeScript definitions</a> and the <a href=\"/docs/en/exemples/#references-v2\">working reference widgets</a>.</p>"
      }
    ]
  },
  {
    "key": "media",
    "slug": "api-media",
    "group": "sdk-reference",
    "label": "Media",
    "title": "Media playback",
    "description": "Read native media information and use the controls offered by the current player.",
    "headings": [
      {
        "id": "permissions",
        "title": "Permissions"
      },
      {
        "id": "example",
        "title": "Minimal example"
      },
      {
        "id": "data-and-controls",
        "title": "Track information and controls"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">Show the track exposed by the native media service and let the user control playback. The available buttons depend on what the current player supports.</p><h2 id=\"permissions\">Permissions</h2><p>Merge these fields into your manifest. <code>media.read</code> reads track information; <code>media.control</code> enables playback commands. For a display-only widget, remove the control permission. These sensitive permissions require an empty network list and <code>clipboardWrite: false</code>.</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json (fragment)",
        "source": "{\n  \"apiVersion\": \"2\",\n  \"permissions\": {\n    \"network\": [],\n    \"clipboardWrite\": false,\n    \"capabilities\": [\n      \"media.read\",\n      \"media.control\"\n    ]\n  }\n}"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"example\">Minimal example</h2><p>This module creates track and status labels plus a play/pause button. Put <code>overcrow.js</code> beside it. The button stays disabled while data is missing or stale, or the player does not offer that action.</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\n\nconst track = document.createElement('p');\nconst status = document.createElement('p');\nconst play = document.createElement('button');\nplay.textContent = 'Play / pause';\nplay.disabled = true;\ndocument.body.append(track, status, play);\n\nconst stop = overcrow.media.onSnapshot(snapshot => {\n  const data = snapshot.data;\n  track.textContent = data\n    ? `${data.title ?? 'Untitled'} · ${data.artist ?? 'Unknown artist'}`\n    : snapshot.status;\n  status.textContent = data?.playbackState ?? snapshot.status;\n  if (snapshot.status === 'stale') {\n    status.textContent += ` · stale (${snapshot.sampleAgeMs ?? 'unknown'} ms old)`;\n  }\n  play.disabled = snapshot.status !== 'ready' || !data?.actions.playPause;\n});\nplay.addEventListener('click', async () => {\n  try {\n    status.textContent = (await overcrow.media.playPause()).status;\n  } catch (error) {\n    status.textContent = error.code ?? 'unavailable';\n  }\n});\nwindow.addEventListener('pagehide', stop, {once: true});"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"data-and-controls\">Track information and controls</h2><p><code>title</code>, <code>artist</code> and <code>artworkHandle</code> can be null. <code>playbackState</code> is playing, paused or stopped. Show text with <code>textContent</code> and clear the old track when data becomes unavailable.</p><p><code>actions.previous</code>, <code>actions.playPause</code> and <code>actions.next</code> describe the player’s available controls. Call <code>previous()</code>, <code>playPause()</code> or <code>next()</code> from a user interaction. An available player action still needs the widget’s control permission. Handle errors and use subsequent snapshots for the resulting playback state; <code>accepted</code> only means queued.</p><p>An artwork handle is an opaque identifier, not an image URL. <code>await overcrow.assets.read(handle)</code> returns a local PNG Blob, at most 512 KiB. Create an object URL to display it, revoke that URL when replaced or closed, and discard a late image response if the track or context changed. Never replace the handle with an external URL.</p><p>Sensitive permissions force temporary browser data even when storage is requested. Native session/account changes discard that browser data. Preview playback controls affect fictional data only.</p><p>Continue with <a href=\"/docs/en/api-services/#confidentialite\">shared service rules</a>, the <a href=\"/docs/downloads/overcrow.d.ts\">complete TypeScript definitions</a> and the <a href=\"/docs/en/exemples/#references-v2\">working reference widgets</a>.</p>"
      }
    ]
  },
  {
    "key": "runtime",
    "slug": "api-runtime",
    "group": "sdk-reference",
    "label": "Lifecycle",
    "title": "Lifecycle and messages",
    "description": "Keep state in a controller and react when the widget is hidden.",
    "headings": [
      {
        "id": "roles",
        "title": "View or controller"
      },
      {
        "id": "relais",
        "title": "Share a counter"
      },
      {
        "id": "visibilite",
        "title": "Pause work when hidden"
      },
      {
        "id": "surface",
        "title": "Request a capture"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"roles\">View or controller</h2>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Role set when the SDK loads",
        "source": "overcrow.runtime.role; // 'view', 'controller' or 'unavailable'"
      },
      {
        "kind": "html",
        "html": "<p>Add <code>\"controller\": \"controller.html\"</code> to <code>entrypoints</code> for an optional controller. Both documents import the SDK. A plain browser reports <code>unavailable</code>; native calls reject.</p>\n<h2 id=\"relais\">Share a counter</h2>\n<p>The controller owns the count. A new view sends <code>hello</code> to receive its current value.</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "controller.js",
        "source": "import overcrow from './overcrow.js';\nconst state = { count: 0 };\novercrow.runtime.onMessage(async message => {\n  if (!message || typeof message !== 'object') return;\n  if (message.type === 'increment') state.count += 1;\n  if (message.type === 'hello' || message.type === 'increment') {\n    await overcrow.runtime.send({ type: 'state', count: state.count });\n  }\n});"
      },
      {
        "kind": "html",
        "html": "<p>Subscribe in the view before sending the first message:</p><p>Add this markup to your view’s <code>index.html</code> before loading the module:</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<p id=\"count\">0</p>\n<button id=\"increment\">+1</button>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\novercrow.runtime.onMessage(message => {\n  if (message?.type !== 'state' || !Number.isSafeInteger(message.count)) return;\n  document.querySelector('#count').textContent = String(message.count);\n});\nawait overcrow.runtime.send({ type: 'hello' });\ndocument.querySelector('#increment').addEventListener('click', () => {\n  overcrow.runtime.send({ type: 'increment' }).catch(console.error);\n});"
      },
      {
        "kind": "html",
        "html": "<p><code>send</code> targets the other role. <code>onMessage</code> returns an unsubscribe function. To confirm that a message was processed, include a request ID and send a reply; the <code>send</code> promise alone confirms neither processing nor saving.</p>\n<p>Messages must be plain JSON, at most 1 MiB and 64 levels deep. Functions, cycles, Date, Map, undefined and non-finite numbers are rejected.</p>\n<h2 id=\"visibilite\">Pause work when hidden</h2>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Visibility changes",
        "source": "const stop = overcrow.lifecycle.onVisibility(visible => {\n  document.body.dataset.visible = String(visible);\n  // Pause animations or unnecessary work while hidden.\n});"
      },
      {
        "kind": "html",
        "html": "<p>A hidden view may still have an active controller. Keep necessary state work running and pause unnecessary timers or animations.</p>\n<h2 id=\"surface\">Request a capture</h2>\n<p><code>await overcrow.surface.invalidate()</code> asks OverCrow to refresh the surface. The runtime already observes page changes; reserve this call for a needed refresh rather than a continuous loop.</p>"
      }
    ]
  },
  {
    "key": "storage",
    "slug": "api-stockage",
    "group": "sdk-reference",
    "label": "Storage",
    "title": "Widget storage",
    "description": "Read, write and remove local JSON data with overcrow.storage.",
    "headings": [
      {
        "id": "permission",
        "title": "Keep data between sessions"
      },
      {
        "id": "petits-reglages",
        "title": "Save and read JSON"
      },
      {
        "id": "cache",
        "title": "Limits and errors"
      },
      {
        "id": "recuperation",
        "title": "Protect saved work"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"permission\">Keep data between sessions</h2><p>Use <code>overcrow.storage</code> to save JSON locally. Since SDK 1.3, this API provides an isolated JSON store for each widget. It never reads or changes built-in notes, history, preferences or accounts. Native OverCrow uses IndexedDB. To request persistence, add this to <code>permissions</code> in your manifest:</p>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json · permissions",
        "source": "\"storage\": true"
      },
      {
        "kind": "html",
        "html": "<p>Without this permission, or with sensitive media capabilities, storage is temporary. The Studio and browser preview always use temporary memory, cleared when the preview reloads or its simulated game session changes.</p><h2 id=\"petits-reglages\">Save and read JSON</h2>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\n\nawait overcrow.storage.set('settings', { compact: true });\nconst settings = await overcrow.storage.get('settings');\nconsole.log(settings); // { compact: true }\nawait overcrow.storage.remove('obsolete-setting');"
      },
      {
        "kind": "html",
        "html": "<p><code>get()</code> returns <code>undefined</code> for a missing key; a saved <code>null</code> stays <code>null</code>. <code>set()</code> completes after its transaction commits. Check whether a successful write is temporary or persistent:</p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "Storage mode",
        "source": "const { mode } = await overcrow.storage.getInfo();\nconsole.log(mode); // 'persistent' or 'temporary'"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"cache\">Limits and errors</h2><div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Widget storage\"><table><thead><tr><th>Item</th><th>Limit</th></tr></thead><tbody><tr><td>Keys</td><td>256 keys; 1–128 UTF-8 bytes each.</td></tr><tr><td>Values</td><td>JSON only; 64 KiB per value, 64 nesting levels.</td></tr><tr><td>Operations</td><td>32 pending requests; opening and each transaction time out after 5 seconds.</td></tr></tbody></table></div><p>Catch errors around awaited calls. On <code>storage_quota_exceeded</code> or <code>storage_unavailable</code>, keep the displayed data and report the failure. <code>invalid_storage_key</code> and <code>invalid_storage_value</code> indicate invalid inputs. Other codes are <code>busy</code>, <code>invalid_response</code>, <code>stale_context</code> and <code>bridge_unavailable</code>. An older host without this API returns <code>unsupported</code>; update OverCrow.</p><h2 id=\"recuperation\">Protect saved work</h2><ul><li>Keep the widget ID stable. Validate and version stored documents; preserve unreadable data for recovery.</li><li>A separate <code>get()</code> then <code>set()</code> is not atomic. Centralize concurrent writes in the controller.</li><li>Persistence is not a backup. SDK data is not included in exported packages or Studio project drafts.</li></ul><p>Existing localStorage/IndexedDB data is not migrated or deleted. Advanced IndexedDB use remains subject to the native policy; use a separate database, never <code>overcrow.sdk.storage</code>. Raw IndexedDB is unavailable in the online Studio. Never store credentials.</p>"
      }
    ]
  },
  {
    "key": "language",
    "slug": "api-langue",
    "group": "sdk-reference",
    "label": "Languages",
    "title": "Widget languages",
    "description": "Use the OverCrow language preference with your translations.",
    "headings": [
      {
        "id": "declaration",
        "title": "Declare your languages"
      },
      {
        "id": "rendu",
        "title": "Display translated text"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"declaration\">Declare your languages</h2>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "manifest.json fragment",
        "source": "\"localization\": {\n  \"defaultLocale\": \"en\",\n  \"availableLocales\": [\"en\", \"fr\"]\n}"
      },
      {
        "kind": "html",
        "html": "<p>Declare 1–16 unique languages, including the default. Forms include <code>fr</code>, <code>en</code> and <code>en-US</code>. OverCrow uses an exact match for the Control Center preference, or the default otherwise.</p>\n<h2 id=\"rendu\">Display translated text</h2>\n<p>Supply a dictionary for each language. Here both modules export an object with a <code>ready</code> label:</p><p>Add this markup to your view’s <code>index.html</code> before loading the module:</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<p id=\"status\" role=\"status\"></p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js · local dictionaries",
        "source": "import overcrow from './overcrow.js';\nimport en from './locales/en.mjs';\nimport fr from './locales/fr.mjs';\nconst messages = { en, fr };\nfunction render(locale) {\n  document.querySelector('#status').textContent = (messages[locale] ?? messages.en).ready;\n}\nconst stop = overcrow.locale.onChanged(render);\nrender(await overcrow.locale.getCurrent());"
      },
      {
        "kind": "html",
        "html": "<p>Call <code>stop()</code> when removing the component. Without <code>localization</code>, <code>getCurrent()</code> returns <code>null</code> and no language-change event is emitted. You provide widget translations; marketplace listing translations are separate.</p>"
      }
    ]
  },
  {
    "key": "clipboard",
    "slug": "api-presse-papiers",
    "group": "sdk-reference",
    "label": "Clipboard",
    "title": "Copy to the clipboard",
    "description": "Copy prepared text from a click in interactive mode.",
    "headings": [
      {
        "id": "permission",
        "title": "Declare the permission"
      },
      {
        "id": "geste",
        "title": "Copy on click"
      },
      {
        "id": "resultat",
        "title": "Show the result"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<h2 id=\"permission\">Declare the permission</h2>"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "Inside permissions",
        "source": "\"clipboardWrite\": true"
      },
      {
        "kind": "html",
        "html": "<h2 id=\"geste\">Copy on click</h2>\n<p>Prepare the text before the click and call <code>writeText</code> directly from the handler.</p><p>Add this markup to your view’s <code>index.html</code> before loading the module:</p>"
      },
      {
        "kind": "code",
        "language": "html",
        "title": "index.html",
        "source": "<button id=\"copy\">Copy</button>\n<p id=\"status\" role=\"status\"></p>"
      },
      {
        "kind": "code",
        "language": "javascript",
        "title": "view.js",
        "source": "import overcrow from './overcrow.js';\ndocument.querySelector('#copy').addEventListener('click', async () => {\n  const status = document.querySelector('#status');\n  try {\n    await overcrow.clipboard.writeText('Hello from OverCrow');\n    status.textContent = 'Copied';\n  } catch {\n    status.textContent = 'Copy denied. Try again in interactive mode.';\n  }\n});"
      },
      {
        "kind": "html",
        "html": "<p>Each current user gesture in interactive mode authorizes one write. Waiting for a network response can expire it. Text is limited to 16 KiB and cannot contain NUL.</p>\n<h2 id=\"resultat\">Show the result</h2>\n<p>Show “Copied” only after success. On refusal, let the user retry. Test gestures in OverCrow; browser previews cannot validate them. The SDK supports writing, with no clipboard read access.</p>"
      }
    ]
  },
  {
    "key": "development",
    "slug": "developpement",
    "group": "test-and-package",
    "label": "Test",
    "title": "Test your widget",
    "description": "Inspect the native engine, replay events and test the game overlay.",
    "headings": [
      {
        "id": "kit-natif",
        "title": "Test with the creator kit"
      },
      {
        "id": "boucle",
        "title": "Use the Linux CLI"
      },
      {
        "id": "watch",
        "title": "Reload a prepared folder"
      },
      {
        "id": "serveur",
        "title": "Load from a local server"
      },
      {
        "id": "replay",
        "title": "Replay test events"
      },
      {
        "id": "devtools",
        "title": "Inspect errors"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "\n<h2 id=\"kit-natif\">Test with the creator kit</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "From a creator-kit project",
        "source": "npm run dev -- --native --devtools"
      },
      {
        "kind": "html",
        "html": "<p>The kit validates sources, recalculates hashes and reloads the native view. The controller keeps running; restart after changing its code. Use this offscreen session for engine checks. Import a package to test the game overlay. For layout, use <a href=\"/docs/en/kit-createur/\">the kit and its reloading browser preview</a>.</p>\n<h2 id=\"boucle\">Use the Linux CLI</h2><p>Build your framework if necessary, then prepare its static output and register that output with the native CLI. Permissions remain explicit; use a fresh private output directory for each preparation.</p>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Linux · output parent must be private (0700)",
        "source": "mkdir -m 700 /absolute/path/output\novercrow-widget prepare /absolute/path/dist /absolute/path/output/build-1\novercrow-widget dev --watch --devtools /absolute/path/output/build-1"
      },
      {
        "kind": "html",
        "html": "\n<h2 id=\"watch\">Reload a prepared folder</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "File notification reload",
        "source": "overcrow-widget dev --watch /absolute/path/widget"
      },
      {
        "kind": "html",
        "html": "\n<p>The watcher groups edit bursts and accepts only consistent manifest and hashes. Invalid changes keep the last valid view and controller. Run your framework build yourself. Use <code>npm run dev -- --native</code> to recalculate hashes automatically.</p>\n<p>Expanded permissions stop the session with <code>capability_confirmation_required</code>. Review the new manifest and start a fresh registration.</p>\n<h2 id=\"serveur\">Load from a local server</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Server must supply consistent manifest and files",
        "source": "overcrow-widget dev --url http://127.0.0.1:4173"
      },
      {
        "kind": "html",
        "html": "\n<p>Use a numeric loopback address and explicit port; <code>localhost</code> is not accepted. The host downloads and freezes verified files. The widget does not connect directly to the server; this is not an HMR or WebSocket tunnel. <code>--watch</code> cannot be combined with <code>--url</code>.</p>\n<h2 id=\"replay\">Replay test events</h2>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Creator kit · Windows and Linux",
        "source": "npm run dev -- --native --replay ./events.json"
      },
      {
        "kind": "code",
        "language": "json",
        "title": "events.json · example fixture",
        "source": "{\n  \"schemaVersion\": 1,\n  \"events\": [\n    { \"atMs\": 0, \"event\": \"overcrow.game.session.v1\",\n      \"payload\": { \"running\": true, \"elapsedMs\": 0 } },\n    { \"atMs\": 1500, \"event\": \"overcrow.game.session.v1\",\n      \"payload\": { \"running\": true, \"elapsedMs\": 1500 } }\n  ]\n}"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Declare event in permissions.gameEvents",
        "source": "overcrow-widget dev --watch --replay /absolute/path/events.json /absolute/path/widget"
      },
      {
        "kind": "html",
        "html": "\n<p>Events follow file order, with non-decreasing timestamps. Limits are 1 MiB and 1,024 events. Replay runs again after a successful reload. Here <code>elapsedMs</code> is a fixture field, not the native snapshot's <code>sessionElapsedMs</code>.</p>\n<h2 id=\"devtools\">Inspect errors</h2><p><code>--devtools</code> opens local developer tools. The terminal reports replacement failures; the inspector shows engine details. Console output can contain private data. Remove tokens and personal responses before sharing a report.</p>"
      }
    ]
  },
  {
    "key": "publication",
    "slug": "publication",
    "group": "test-and-package",
    "label": "Distribute",
    "title": "Distribute a widget",
    "description": "Export a package and prepare its marketplace listing.",
    "headings": [
      {
        "id": "avant-export",
        "title": "Before export"
      },
      {
        "id": "paquet",
        "title": "Build a package"
      },
      {
        "id": "fiche",
        "title": "Prepare a listing"
      },
      {
        "id": "admission",
        "title": "Review and installation"
      },
      {
        "id": "licences",
        "title": "Licenses"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "\n<h2 id=\"avant-export\">Before export</h2><ul><li>Test small sizes, zoom, passive and interactive modes.</li><li>Handle loading, empty data, failure and recovery.</li><li>Check saving and returning after the view is hidden.</li><li>Include every local resource and license in the prepared bundle.</li></ul>\n<h2 id=\"paquet\">Build a package</h2><p>Run <code>npm run package</code> in a Windows/Linux creator-kit project. The command prints the output path and recalculates hashes. <a href=\"/docs/en/kit-createur/\">See the complete flow</a>. With the Linux native CLI:</p>"
      },
      {
        "kind": "code",
        "language": "sh",
        "title": "Linux · writes .ocpkg beside the folder",
        "source": "overcrow-widget package /absolute/path/prepared-widget"
      },
      {
        "kind": "html",
        "html": "\n<p>The command validates manifest and files and creates a deterministic stored ZIP with the .ocpkg extension. It neither signs nor installs nor publishes. Changes to bytes require re-preparation, and each new published widget version needs a new version number.</p>\n<h2 id=\"fiche\">Prepare a listing</h2><p><code>listing.json</code> describes the author, license, source and catalog translations. It is separate from runtime files. Listing translations do not declare languages actually supplied by the widget.</p>\n<p><a href=\"https://github.com/Valhallab/playervox-overcrow-releases/blob/main/docs/creator-guide.md\" rel=\"noreferrer\">Read the marketplace creator guide ↗</a> and <a href=\"https://github.com/Valhallab/playervox-overcrow-releases/blob/main/docs/review-policy.md\" rel=\"noreferrer\">review policy ↗</a>.</p>\n<p><strong>Marketplace screenshot.</strong> Add a PNG to your widget, for example <code>widget/preview.png</code>, before preparing its package. In the publication sidecar <code>listing.json</code>, set <code>\"preview\": \"preview.png\"</code> or another relative path such as <code>\"preview\": \"images/screenshot.png\"</code>. The image must appear in the generated <code>manifest.files</code> ledger. Use a static PNG of at most 256 KiB and 1,024 × 1,024 pixels; 960 × 600 is a useful landscape format. URLs, SVGs, animated images and paths outside the widget are rejected.</p><p>The marketplace displays the admitted image on cards and detail pages, with a neutral placeholder if it is absent. Changing a published screenshot requires a new widget version and signed publication.</p>\n<h2 id=\"admission\">Review and installation</h2><p>Review and publication use the validated listing, manifest and exact package bytes. The signed catalog and installation consent are separate from local development. “Open in OverCrow” opens a listing in Control Center; it does not install or activate a widget automatically.</p>\n<h2 id=\"licences\">Licenses</h2><p>The SDK and examples here are MIT-licensed; keep their notices. They do not force you to license your own widget under MIT. The public policy for admitting third-party licenses is still pending; successful format validation alone does not authorize publication.</p>"
      }
    ]
  },
  {
    "key": "twitch",
    "slug": "api-twitch",
    "group": "troubleshooting",
    "label": "Twitch migration",
    "title": "Twitch integration removed",
    "description": "The public SDK no longer shares OverCrow’s built-in Twitch connection.",
    "headings": [
      {
        "id": "permissions",
        "title": "Remove old permissions"
      },
      {
        "id": "example",
        "title": "Remove old SDK calls"
      },
      {
        "id": "write-and-reply",
        "title": "Messages and replies"
      },
      {
        "id": "connection-and-messages",
        "title": "Connected accounts"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "<p class=\"lead\">The public SDK has no Twitch integration. Widgets cannot access the built-in Twitch account, chat state or native connection.</p><h2 id=\"permissions\">Remove old permissions</h2><p>Remove <code>twitch.chat.read</code> and <code>twitch.chat.compose</code> from existing manifests.</p><h2 id=\"example\">Remove old SDK calls</h2><p>Remove <code>overcrow.twitch</code> calls. The Twitch reference template and download are no longer distributed.</p><h2 id=\"write-and-reply\">Messages and replies</h2><p>The SDK provides no shared chat, native composer or reply action.</p><h2 id=\"connection-and-messages\">Connected accounts</h2><p>OverCrow’s built-in Twitch login and tokens remain private to the application. There is no replacement public SDK API in this release. See the <a href=\"/docs/en/api-services/\">available host services</a>.</p>"
      }
    ]
  },
  {
    "key": "troubleshooting",
    "slug": "depannage",
    "group": "troubleshooting",
    "label": "Troubleshooting",
    "title": "Troubleshooting",
    "description": "Find an error and the next step to try.",
    "headings": [
      {
        "id": "erreurs",
        "title": "Error codes"
      },
      {
        "id": "isoler",
        "title": "Check three things"
      },
      {
        "id": "signaler",
        "title": "Report an issue"
      },
      {
        "id": "limites",
        "title": "Runtime limits"
      }
    ],
    "blocks": [
      {
        "kind": "html",
        "html": "\n<h2 id=\"erreurs\">Error codes</h2><div class=\"table-scroll\" tabindex=\"0\" role=\"region\" aria-label=\"Symptom / code, Likely cause, Next step\"><table><thead><tr><th>Symptom / code</th><th>Likely cause</th><th>Next step</th></tr></thead><tbody><tr><td>bridge_unavailable</td><td>The SDK cannot find the native runtime.</td><td>Use dev or an installed package; a plain web server has no native bridge.</td></tr><tr><td>invalid_manifest / invalid_bundle / invalid widget project</td><td>Schema, path, file or hash is inconsistent.</td><td>Check entrypoints and permissions, then prepare a fresh output.</td></tr><tr><td>unsafe_path</td><td>Relative path, symlink or incompatible permissions.</td><td>Use a real absolute path and private 0700 output parent.</td></tr><tr><td>capability_confirmation_required</td><td>Reload requests more permissions.</td><td>Review the change and explicitly restart development.</td></tr><tr><td>capability_denied / permission_denied</td><td>Permission or current user gesture is missing or expired.</td><td>Check manifest and consent; click again in interactive mode for clipboard.</td></tr><tr><td>body_used</td><td>The same response body was read twice.</td><td>Read json(), text() or arrayBuffer() once and store the result.</td></tr><tr><td>invalid_message / message_too_large</td><td>Message is not small, plain JSON.</td><td>Avoid cycles and split large application data.</td></tr><tr><td>request_body_limit</td><td>Request body exceeds 2 MiB.</td><td>Reduce the request or split it if the API permits.</td></tr><tr><td>busy / unresponsive</td><td>Operation overlaps or runtime did not respond in time.</td><td>Disable duplicate actions, then offer a retry.</td></tr><tr><td>Blank view after an edit</td><td>JavaScript error, missing entrypoint or unprepared build.</td><td>Open devtools, check index.html and prepare again.</td></tr><tr><td>Session events do not fire</td><td>Declaring a name does not create a live feed.</td><td>Use onSnapshot for native updates; check replay event names/payloads.</td></tr><tr><td>HTTP 4xx / 5xx</td><td>Remote service refused or failed.</td><td>Check response.ok, show an error and retain last valid data.</td></tr></tbody></table></div>\n<h2 id=\"isoler\">Check three things</h2><ol><li>Does the HTML render with local assets at the expected size?</li><li>Is the widget running in OverCrow with required permissions?</li><li>Does received data match the schema your code expects?</li></ol>\n<h2 id=\"signaler\">Report an issue</h2><p>Include OverCrow, widget and SDK versions; platform; minimal steps; expected versus actual result; and error code. Use a small example or non-personal fixture. Application diagnostics can prepare a report for explicit submission.</p>\n<h2 id=\"limites\">Runtime limits</h2><p>The SDK has no shell, Node, Tauri, arbitrary file access or game-memory reading. External browser network access is blocked. An invented manifest permission cannot open those boundaries.</p>"
      }
    ]
  }
]
